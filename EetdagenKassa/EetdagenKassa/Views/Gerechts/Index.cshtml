@model IEnumerable<EetdagenKassa.Models.Gerecht>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Omschrijving)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prijs)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Omschrijving)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prijs)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GerechtID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GerechtID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GerechtID })
        </td>
    </tr>
}

</table>
